const Canvas = require("canvas");

module.exports = {
    name: "info-module",
    description: "Module for generate banner when user join/boost the server.",
    execute(member) {

        const canvas = Canvas.createCanvas(700, 250);

    }
}